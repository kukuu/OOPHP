/* EPD 4891 LAB5
   Mr. Oguzhan KOKSAL 9600275
   MSc TELEMATICS
   MIDDLESEX UNIVERSITY
*/

--ANSWER 1
SELECT	ENAME,DNAME
FROM	EMP,DEPT
WHERE	EMP.DEPTNO = DEPT.DEPTNO;

--ANSWER 2
SELECT	ENAME,E.DEPTNO,DNAME
FROM	EMP E, DEPT D
WHERE	E.DEPTNO = D.DEPTNO;

--ANSWER 3
SELECT	ENAME, LOC LOCATION, DNAME
FROM	EMP, DEPT
WHERE	EMP.DEPTNO = DEPT.DEPTNO
AND	SAL > 1500;

--ANSWER 4
SELECT	ENAME, JOB, SAL, GRADE
FROM	EMP,SALGRADE
WHERE	SAL BETWEEN LOSAL AND HISAL;

--ANSWER 5
SELECT	ENAME, JOB, SAL, GRADE
FROM	EMP,SALGRADE
WHERE	SAL BETWEEN LOSAL AND HISAL
AND	GRADE=3;

--ANSWER 6
SELECT	ENAME, SAL, LOC LOCATION
FROM	EMP,DEPT
WHERE	EMP.DEPTNO = DEPT.DEPTNO
AND	LOC ='DALLAS';

--ANSWER 7
SELECT	ENAME, JOB, SAL, GRADE, DNAME
FROM	EMP, SALGRADE, DEPT
WHERE	EMP.DEPTNO = DEPT.DEPTNO
AND	SAL BETWEEN LOSAL AND HISAL
AND	JOB !='CLERK'
ORDER BY SAL DESC;

--ANSWER 8
SELECT	ENAME,JOB,SAL* 12 ANNUAL_SAL,D.DEPTNO,DNAME,GRADE
FROM	EMP E, SALGRADE, DEPT D
WHERE	E.DEPTNO = D.DEPTNO
AND	SAL BETWEEN LOSAL AND HISAL
AND	(SAL* 12+NVL(COMM,0) = 36000
OR	E.JOB ='CLERK')
ORDER BY E.JOB;


--ANSWER 9
SELECT	D.DEPTNO,DNAME
FROM	EMP E,DEPT D
WHERE	E.DEPTNO(+) = D.DEPTNO
AND	E.EMPNO IS NULL;

--ANSWER 10
SELECT	EMPS.EMPNO,EMPS.ENAME,MGRS.EMPNO MGRNO,
MGRS.ENAME MGR_NAME
FROM	EMP EMPS, EMP MGRS
WHERE	EMPS.MGR = MGRS.EMPNO;


--ANSWER 11
SELECT	EMPS.EMPNO,EMPS.ENAME,MGRS.EMPNO MGRNO,
MGRS.ENAME MGR_NAME
FROM	EMP EMPS, EMP MGRS
WHERE	EMPS.MGR = MGRS.EMPNO(+);

--ANSWER 12
SELECT	JOB
FROM	EMP
WHERE	HIREDATE BETWEEN'01-JAN-83'AND'30-JUN-83'
INTERSECT
SELECT	JOB
FROM	EMP
WHERE	HIREDATE BETWEEN'01-JAN-84'AND'30-JUN-84';

--ANSWER 13
SELECT	E.ENAME EMPLOYEE,E.HIREDATE,M.ENAME MANAGER,
M.HIREDATE
FROM	EMP E, EMP M
WHERE	E.MGR = M.EMPNO
AND	E.HIREDATE < M.HIREDATE;


--ANSWER 14
SELECT	DEPTNO, DNAME
FROM	DEPT
MINUS
SELECT	EMP.DEPTNO, DNAME
FROM	EMP, DEPT
WHERE	EMP.DEPTNO = DEPT.DEPTNO;
 